# NLP Text Classification

End-to-end text classification pipeline demonstrating preprocessing, feature extraction, model training, and evaluation.

Quickstart
----------

1. Create venv and install:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

2. Run the demo:

```bash
python -m src.pipeline
```
